/**
 * This package contains the service layer of the application, which includes business logic.
 * The service layer acts as an intermediary between the controllers and the data access layer.
 * It is responsible for processing data, applying business rules, and coordinating the flow of information
 */

package app.service;
