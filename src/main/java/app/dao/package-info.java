/**
 * This package contains the Data Access Object (DAO) classes responsible for interacting with the database.
 * The DAO classes provide methods for performing CRUD (Create, Read, Update, Delete) operations on the application's data models.
 * Each DAO class corresponds to a specific entity in the database and encapsulates the logic for accessing and manipulating that entity's data.
 */
package app.dao;
